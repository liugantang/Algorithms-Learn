二叉搜素树采用了链表
在其java实现中，其核心部分是put()与get()的实现
get()实现是很简单的，二叉搜素树的特点是一个节点的左子树的所有节点的键值小于该节点的键值，一个节点的右子树的所有节点的键值大于该节点，于是我们可以利用
这个特性，使用递归的方法，得到一种类似于二分搜索的算法。
put()要稍微复杂一些
它首先会判断节点是否为空，如果是空节点，那就返回一个新节点
之后又是利用在get方法中使用的二叉搜索树的特点，利用递归，找到新的节点所应在的位置，再把返回的新节点挂到这个位置
如果已经存在了要插入的键值，那么就更新该键值所对应值的值
然后依然是递归地计算出树的大小，之后把新的树整个返回
注意，在示例代码中，利用java的重载功能，写了两个get两个put，这可以更好地更新与赋值。
