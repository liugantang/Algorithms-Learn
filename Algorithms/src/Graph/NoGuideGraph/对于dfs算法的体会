很少有像dfs算法这样既简洁又高效的算法，理解起来其实也特别容易；
理解dfs算法首先要搞清楚它的原理；
我们使用bag[]构建的无向图每个节点都是一个bag，里面有这个节点所连接的其它节点的下标；
把它们都标记为true；
然后逐一访问其它节点，把其它节点所连接的没有被标记的节点也一一标记，这样循环往复，直到没有可以再标记的节点；
那么其所标记的节点就是它所能够连通的节点；
没有被标记的节点就是不能够连通的节点

